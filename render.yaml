services:
  - type: web
    name: chatgpt-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python process_docs.py"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
